services:
  mock-server:
    build:
      context: .
      dockerfile: mock-server/Dockerfile
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${BACKEND_SERVER_PORT}:${BACKEND_SERVER_PORT}"
    networks:
      - server_network

networks:
  server_network:
    driver: bridge
